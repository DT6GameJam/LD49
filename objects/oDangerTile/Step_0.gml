/// @description Insert description here
// You can write your code in this editor

if(place_meeting(x,y, oPlayer) && alarmTimer = false && counter = 0)
{
	alarmTimer = true;
	alarm[0] = room_speed * 2;
}
if(place_meeting(x,y, oPlayer) && alarmTimer = false && counter = 1)
{
	alarmTimer = true;
	alarm[1] = room_speed * 2;
}